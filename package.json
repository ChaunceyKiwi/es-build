{
  "name": "es-build",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild app.jsx --bundle --outfile=out.js",
    "start": "esbuild app.jsx --outdir=www/js --bundle --servedir=www --watch"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "0.21.5"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
